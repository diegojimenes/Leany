# Application
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=leany

# Note: If running via Docker Compose, 
# DB_HOST should be the service name (e.g., DB_HOST=db)
